data_sources:
  - dataset_name: rsna
    data_zip: processed_datasets/rsna/rgb_multiwindow.zip
    row_key: processed_path_stem
  - dataset_name: spr
    data_zip: processed_datasets/spr/rgb_multiwindow.zip
  - dataset_name: vindr
    data_zip: processed_datasets/vindr/rgb_multiwindow.zip

labels_csv: processed_datasets/combined_mammo_recall_labels_birads_only.csv
split_csv: processed_datasets/combined_mammo_recall_splits_spr_holdout.csv
split_col: experiment_split
metadata_member: metadata.csv
label_key: image_id
row_key: image_id
label_col: target
patient_col: patient_id
dataset_col: dataset
labels_dataset_col: dataset
max_samples:
require_all_labels: false

run_dir: runs/all_rgb_multiwindow_convnext_tiny_1024_weighed_loss
model: convnext_tiny
input_channels: 3
image_size: 1024

epochs: 10
batch_size: 4
lr: 0.0001
weight_decay: 0.0001
loss_weighting: balanced
val_fraction: 0.2
num_workers: 2
seed: 42
device: auto
pretrained: true

wandb: true
wandb_project: mammorecall
wandb_entity:
wandb_run_name: weighed-loss
