data_zip: data/processed/rgb_multiwindow.zip
labels_csv: data/labels.csv
metadata_member: metadata.csv
label_key: image_id
label_col: label
patient_col: patient_id

run_dir: runs/local_rgb_multiwindow_simple_cnn
model: simple_cnn
input_channels: 3
image_size: 512

epochs: 3
batch_size: 4
lr: 0.001
weight_decay: 0.0001
val_fraction: 0.2
num_workers: 2
seed: 42
device: auto
pretrained: false

wandb: false
wandb_project: mammorecall
wandb_entity:
wandb_run_name: local-rgb-multiwindow-simple-cnn
