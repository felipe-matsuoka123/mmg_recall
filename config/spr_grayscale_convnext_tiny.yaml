data_zip: processed_datasets/spr/grayscale.zip
data_root:
metadata_csv:
labels_csv: combined_mammo_recall_labels_birads_only.csv
metadata_member: metadata.csv
label_key: image_id
label_col: target
patient_col: patient_id
dataset_name: spr
dataset_col: dataset
labels_dataset_col: dataset
max_samples:
require_all_labels: false

run_dir: runs/spr_grayscale_convnext_tiny
model: convnext_tiny
input_channels: 1
image_size: 512

epochs: 10
batch_size: 8
lr: 0.0001
weight_decay: 0.0001
val_fraction: 0.2
num_workers: 4
seed: 42
device: auto
pretrained: true

wandb: true
wandb_project: mammorecall
wandb_entity:
wandb_run_name: spr-grayscale-convnext-tiny
