name: mammorecall

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - numpy
  - pytest
  - pip
  - pip:
      - pydicom
      - opencv-python
      - jupyter
      - matplotlib
      - tqdm